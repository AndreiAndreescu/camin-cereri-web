import "./globals.css";

export const metadata = {
  title: "Cămin Romantic — Cereri produse",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ro">
      <body>{children}</body>
    </html>
  );
}
